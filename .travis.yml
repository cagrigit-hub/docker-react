sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t cakitomakito/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cakitomakito/docker-react npm run test
